Filter_ocaml 
------------

une activiteé "Filter", les entrées sont l'id, t1, t2, t3

a.out calcule la somme t1+t2 et met la résultat sous t3

marche pas à cause de:

	Fatal error: the file './a.out' is not a bytecode executable file
_________________________________________________________________________

Filter_Spoc 
-----------
une activiteé "Filter", les entrées sont l'id, f1, f2

Chiron_Add.byte: met la contenu des fichiers f1, f2 dans des vecteurs, il fait l'addition entre les deux vecteurs, et il met la résultat dans f3

marche pas à cause de:

	org.postgresql.util.PSQLException: ERROR: missing data for column "f1"
  Où : COPY iact1, line 4: ""

________________________________________________________________________


Add_Spoc
------------
avec un kernel pour un seul calcul en Spoc, les entrées sont l'id, t1, t2, t3

calcule la somme t1+t2 et met la résultat sous t3

marche pas à cause de :

	Fatal error: the file './Add1_Chiron.byte' is not a bytecode executable file

________________________________________________________________________

_Apparament il accepte que des bash command
